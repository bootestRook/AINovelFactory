name: ai_novel_factory
description: AI novel writing desktop app.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  archive: ^4.0.9
  file_selector: ^1.1.0
  flutter:
    sdk: flutter
  sqflite_common_ffi: ^2.3.3

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
